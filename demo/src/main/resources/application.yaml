spring:
  application:
    name: camunda-jsonforms-demo
server:
  port: 8080    
camunda:
  bpm:
    admin-user:
      id: demo
      password: demo
    database:
      type: h2

logging:
  level:
    org.camunda.bpm.client: info